#Konfigurasi Server
PORT=3000
NODE_ENV=development

#JWT secret --ganti dengan string acak yang panjang!
#cara generate node -e
"console.log(require('crypto').randomBytes(64).toString('hex))"
JWT_SECRET=MY_SUPER_SECRET_KEY
JWT EXPIRES IN=7d
